# импорт встроенных пакетов

import random # генератор случайных событий
import time # работы со временем
import os # взаимодействие файла с файловой системой


while True: 
    print(random.randint(0, 100))
    time.sleep(3) # Ctrl+C чтобы остановить

# Импорт внешних пакетов
# предварительно нужно их установить
# например, pip install line_profiler
    